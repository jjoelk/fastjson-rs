[package]
name = "fastjson-rs"
version = "0.2.1"
edition = "2021"
description = "Blazing fast JSON parser with zero-copy deserialization"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
